#:import AddNote screens.add_note.AddNote
#:import AllNotes screens.all_notes.AllNotes
#:import ShowNote screens.show_note.ShowNote
#:import LoadingScreen screens.loading_screen.LoadingScreen
#:import NoTransition kivy.uix.screenmanager.NoTransition

ScreenManager:
    transition: NoTransition()

    AllNotes:
        name: "all_notes"
        id: all_notes

    AddNote:
        name: "add_note"
        id: add_note

    ShowNote:
        name: "show_note"
        id: show_note

    LoadingScreen:
        name: "loading"
